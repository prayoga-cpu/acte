services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: acte
      POSTGRES_PASSWORD: acte
      POSTGRES_DB: acte
    ports:
      - "5432:5432"
    volumes:
      - acte_pg:/var/lib/postgresql/data
volumes:
  acte_pg:
